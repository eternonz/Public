# Generated by ProjectRepoSync — per-project disposable exclusions
.DS_Store
.wrangler/
.venv/
__pycache__/
*.pyc
.build/
logs/
# runtime logs and browser-session credentials must never be versioned
# (Codex R13 P0-5 / P2 — the generator, not just the file, so it persists)
*.log
cookies.txt
cookie_header.txt
*.cookies
cookies*.txt
# ProjectArchive runtime state (never project source)
archive.lock
archive-transaction.json
